function isInViewport(element){const rect=element.getBoundingClientRect();return(rect.top>=0&&rect.top<=(window.innerHeight||document.documentElement.clientHeight))}
function animateScoreNumbers(){const scoreNumbers=document.querySelectorAll('.score-number');scoreNumbers.forEach(scoreNumber=>{const target=+scoreNumber.getAttribute('data-target');const increment=target/100;let current=0;const updateScore=()=>{scoreNumber.innerText=Math.round(current);if(current<target){current+=increment;setTimeout(updateScore,20)}else{scoreNumber.innerText=target}}
updateScore()})}
function checkViewport(){const section=document.getElementById('competitive-programming');if(isInViewport(section)){animateScoreNumbers();window.removeEventListener('scroll',checkViewport)}}
window.addEventListener('scroll',checkViewport)